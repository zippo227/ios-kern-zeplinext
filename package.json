{
  "name": "iOS-kern",
  "version": "0.1.0",
  "description": "shows kern value if letter spacing is set",
  "scripts": {
    "start": "zem start",
    "build": "zem build",
    "clean": "zem clean",
    "exec": "zem exec"
  },
  "zeplin": {
    "displayName": "iOS Kerning",
    "projectTypes": ["ios"]
  },
  "dependencies": {
    "zem": "^0.1.2"
  }
}
